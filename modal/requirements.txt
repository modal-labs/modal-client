# Pins Modal dependencies installed within the container runtime.
aiohttp==3.8.1
aiostream==0.4.4
certifi==2021.10.8
cloudpickle==2.0.0
cuda-python==11.7.1
fastapi==0.75.2
fastprogress==1.0.0

### TODO(akshat): Remove this `grpcio` dependency. This is needed for backwards-compatibility with client versions
### <=0.0.23, since we don't version our base images currently, so the base image needs to still work for _all_
### currently supported client versions unfortunately.
grpcio==1.48.0rc1

grpclib==0.4.3
importlib_metadata==4.8.1
protobuf>=3.19.4
python-multipart>=0.0.5
rich==12.3.0
sentry-sdk==1.5.1
synchronicity==0.2.7
toml==0.10.2
typer==0.4.1
types-certifi==2021.10.8.3
types-toml==0.10.4
