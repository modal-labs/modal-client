# Changelog for Unreleased User-facing Updates

**When releasing, move these changelog items to `CHANGELOG.md`.**

- The [`modal.Environment.members.list()`](/docs/sdk/py/latest/modal.Environment#memberslist) method (and the [`modal environment members list`](/docs/cli/latest/environment#modal-environment-members-list) CLI) now returns every workspace member and service user together with their effective role in the Environment
